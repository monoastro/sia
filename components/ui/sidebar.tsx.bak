"use client";
import Link from 'next/link';
import { usePathname } from 'next/navigation';

const navItems = [
  { href: '/application', label: 'Home', icon: '🏠' },
  { href: '/application/announcements', label: 'Announcements', icon: '📢' },
  { href: '/application/routine', label: 'Routine', icon: '📅' },
  { href: '/application/semesters', label: 'Semesters', icon: '🎓' },
  { href: '/application/generalChats', label: 'General Chats', icon: '💬' },
];

const Sidebar = () => {
  const pathname = usePathname();

  return (
    <nav className="w-64 bg-gray-800 text-white h-screen flex flex-col">
      <div className="p-4">
        <h1 className="text-2xl font-bold">Your App</h1>
      </div>
      <ul className="flex-1">
        {navItems.map((item) => (
          <li key={item.href}>
            <Link href={item.href}>
              <span className={`flex items-center p-4 hover:bg-gray-700 ${pathname === item.href ? 'bg-gray-700' : ''}`}>
                <span className="mr-3">{item.icon}</span>
                {item.label}
              </span>
            </Link>
          </li>
        ))}
      </ul>
    </nav>
  );
};

export default Sidebar;
